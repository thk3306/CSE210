// I Have exceeded the requirements of the assignment by creating a Bad Habit goal type which is a negative goal that deducts points when completed.
using System;

class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();
        goalManager.Start();
    }
}
